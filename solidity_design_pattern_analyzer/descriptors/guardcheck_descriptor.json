{
    "name": "Guard Check",
    "checks": [
        {
            "check_type": "fn_call",
            "callable_function": [
                "_regex:require\\(.*\\)", "_regex:assert\\(.*\\),", "_regex:revert\\(.*\\)"
            ]
        }
    ]
}