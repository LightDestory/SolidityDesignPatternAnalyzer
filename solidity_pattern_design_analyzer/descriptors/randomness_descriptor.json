{
    "name": "Randomness",
    "checks": [
        {
            "check_type": "fn_call",
            "callable_function": [
                "uint(blockhash(block.number - 1))",
                "uint(keccak256(abi.encodePacked(blockhash(block.number-1), seed)))",
                "uint(keccak256(abi.encodePacked(block.timestamp, msg.sender, block.difficulty)))"
            ]
        }
    ]
}